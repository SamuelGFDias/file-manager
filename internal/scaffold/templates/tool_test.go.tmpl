// Code generated by the file-manager scaffold generator (cmd/scaffold). DO NOT
// remove this header.
// TODO: add real test cases covering the behavior of {{.Title}} once run()
// is implemented (e.g. success case, missing input, invalid input).
package {{.Package}}

import (
	"strings"
	"testing"
)

func TestMetaID(t *testing.T) {
	got := New().Meta().ID
	want := "{{.Kebab}}"
	if got != want {
		t.Fatalf("Meta().ID = %q, want %q", got, want)
	}
}

func TestCommandUse(t *testing.T) {
	use := New().Command().Use
	want := "{{.Kebab}}"
	if !strings.HasPrefix(use, want) {
		t.Fatalf("Command().Use = %q, want prefix %q", use, want)
	}
}

func TestDocFlagsMatchesParams(t *testing.T) {
	tl := New()

	doc := tl.Doc()
	params := tl.params()

	if len(doc.Flags) != len(params) {
		t.Fatalf("Doc().Flags tem %d entradas, esperava %d (mesmo tamanho de params())", len(doc.Flags), len(params))
	}
}
