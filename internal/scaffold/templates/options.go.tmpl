// Code generated by the file-manager scaffold generator (cmd/scaffold). DO NOT
// remove this header.
// TODO: rename the Input field (and its "input" yaml tag) to the real
// options {{.Title}} needs, and add every other field the tool requires.
package {{.Package}}

// Options holds the configuration for the {{.Title}} tool. It is what gets
// bound to cobra flags, asked in the interactive prompts and, once this tool
// implements tool.ProfileSupport, persisted as a saved profile.
type Options struct {
	// Input is a placeholder option.
	// TODO: replace with the real parameter(s) {{.Title}} needs.
	Input string `yaml:"input"`
}

// defaultOptions returns the default Options for {{.Title}}.
// TODO: fill in real defaults once the actual options are defined.
func defaultOptions() Options {
	return Options{
		Input: "",
	}
}
