// Code generated by the file-manager scaffold generator (cmd/scaffold). DO NOT
// remove this header.
// TODO: write Meta().Description and flesh out Doc() (Summary, Description,
// WhenToUse, Examples, Notes) with real information about {{.Title}}.
// TODO: if {{.Title}} should support saved profiles, implement a type that
// satisfies tool.ProfileSupport (Empty/Edit/Apply) and return it from
// Profile() below instead of nil.
package {{.Package}}

import (
	"github.com/SamuelGFDias/file-manager/internal/tool"
	"github.com/SamuelGFDias/file-manager/internal/ui"
)

// {{.Type}} implements tool.Tool for the "{{.Kebab}}" command.
//
// Command(), params() and run() live in command.go; the interactive screen
// lives in screen.go; Options and its defaults live in options.go.
type {{.Type}} struct {
	opts Options
}

// New creates a new {{.Type}} with its default options.
func New() *{{.Type}} {
	return &{{.Type}}{opts: defaultOptions()}
}

// Meta returns the identification metadata for {{.Title}}.
func (t *{{.Type}}) Meta() tool.Meta {
	return tool.Meta{
		ID:    "{{.Kebab}}",
		Title: "{{.Title}}",
		// TODO: write a one-line description of what {{.Title}} does.
		Description: "TODO: descrever o que {{.Title}} faz",
	}
}

// Screen returns the interactive screen for {{.Title}}.
func (t *{{.Type}}) Screen() ui.Screen {
	return &{{.Type}}Screen{tool: t}
}

// Doc returns the exportable documentation for {{.Title}}.
func (t *{{.Type}}) Doc() tool.Doc {
	return tool.Doc{
		ID:    "{{.Kebab}}",
		Title: "{{.Title}}",
		// TODO: write a one-line summary of {{.Title}}.
		Summary: "TODO: resumo de uma linha de {{.Title}}",
		// TODO: write a paragraph explaining what {{.Title}} does.
		Description: "TODO: parágrafo explicando o comportamento de {{.Title}}",
		// TODO: list the situations where {{.Title}} should be used.
		WhenToUse: []string{
			"TODO: quando o usuário pedir para ...",
		},
		Flags: tool.DocFlags(t.params()),
		Examples: []tool.ExampleDoc{
			// TODO: add at least one real usage example, e.g.:
			// {Title: "TODO", Command: "file-manager {{.Kebab}} --input arquivo"},
		},
		// TODO: add limitations or warnings about {{.Title}}, if any.
		Notes: []string{},
	}
}

// Profile returns nil: {{.Title}} does not support saved profiles yet.
// TODO: to add support, implement tool.ProfileSupport (Empty/Edit/Apply) for
// {{.Type}} (or a dedicated type) and return it here instead of nil.
func (t *{{.Type}}) Profile() tool.ProfileSupport {
	return nil
}
