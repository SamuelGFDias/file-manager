---
name: file-manager
description: "CLI para manipular PDFs em lote — une múltiplos PDFs em um só, separa um PDF em vários arquivos e organiza/renomeia PDFs em lote a partir do conteúdo, usando regras baseadas em regex de texto. Também desfaz organizações (undo), gerencia perfis salvos (profiles), mostra a versão (version) e se atualiza sozinho (update). Use esta skill quando o usuário pedir para unir, separar, organizar ou renomear PDFs em lote, para desfazer uma organização, ou para exportar/importar/listar perfis salvos de uma ferramenta."
---

# file-manager

CLI Go (binário `file-manager`) para manipulação de PDFs. Versão de referência
desta skill: {{.Version}}.
{{range .Tools}}
## {{.Title}}

{{range .WhenToUse}}- {{.}}
{{end}}
Rode:

{{range .Examples}}```bash
{{.Command}}
```

{{end}}
Flags essenciais: {{range $i, $f := .Flags}}{{if $i}}, {{end}}`--{{$f.Name}}`{{end}}
{{end}}
## Comandos auxiliares

Não são "ferramentas" de PDF, mas são comandos reais do binário — cobertura
completa, para não inventar comando nenhum além destes.
{{range .Commands}}
### {{.Title}}

{{range .WhenToUse}}- {{.}}
{{end}}
Rode:

{{range .Examples}}```bash
{{.Command}}
```

{{end}}{{if .Flags}}Flags: {{range $i, $f := .Flags}}{{if $i}}, {{end}}`--{{$f.Name}}`{{end}}
{{end}}
{{end}}
## Regras

- O binário não faz OCR: um PDF digitalizado sem camada de texto não casa com
  regex de busca em conteúdo.
- `organize-pdf` copia os arquivos por padrão; só move (remove a origem) quando
  `--move` é informado.
- Sempre sugira `--dry-run` antes de uma execução real de `organize-pdf`, para o
  usuário conferir o resultado antes de aplicar.
- Nomes de arquivo derivados de regex são sanitizados antes de serem usados no
  sistema de arquivos (caracteres inválidos são removidos/substituídos).
